# Local-only environment file for docker compose and local runs.
# Copy this file to .env and fill in your real key.
# .env is already ignored by Git.

SERVER_PORT=8080
CHRONOSCHED_BASE_URL=http://server:8080
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
